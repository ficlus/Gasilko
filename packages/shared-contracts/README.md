# Shared contracts

Future versioned API schemas and generated database types belong here. There are no domain contracts in M0. Generate types from migrations when M1 introduces the schema; do not invent duplicate hand-maintained domain models.
